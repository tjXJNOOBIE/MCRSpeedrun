<configuration xmlns="http://jgitver.github.io/maven/configuration/1.1.0"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://jgitver.github.io/maven/configuration/1.1.0
                                   https://jgitver.github.io/maven/configuration/jgitver-configuration-v1_1_0.xsd">

    <!-- Use Maven-like versioning (major.minor.patch) -->
    <strategy>MAVEN</strategy>

    <!-- Lookup the latest tag for base version -->
    <policy>LATEST</policy>

    <!-- Automatically increment the patch version for commits since last tag -->
    <autoIncrementPatch>true</autoIncrementPatch>

    <!-- Use commit distance to increment patch version -->
    <useCommitDistance>true</useCommitDistance>

    <!-- Append -SNAPSHOT if commit is not tagged -->
    <useSnapshot>true</useSnapshot>

    <!-- Git commit id length in version metadata (optional) -->
    <gitCommitIdLength>8</gitCommitIdLength>

    <!-- Start from 0.0.0 if no tag exists yet -->
    <regexVersionTag>MCRSpeedrun-v([0-9]+\.[0-9]+\.[0-9]+)</regexVersionTag>

    <!-- Optional exclusions -->
    <exclusions></exclusions>

    <!-- Use default branching policy fallback -->
    <useDefaultBranchingPolicy>true</useDefaultBranchingPolicy>

</configuration>
